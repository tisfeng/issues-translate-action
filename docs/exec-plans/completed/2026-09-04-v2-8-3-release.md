# v2.8.3 Release

- Status: completed
- Date: 2026-09-04
- Owner: Codex

## Request and boundary

Publish the smallest `v2.8.3` release. Rename the existing `v2.8` and
`v2.8.1` GitHub Release titles to their tag names, push the already-approved
local `main` commits, and create `v2.8.3`.

Allowed changes and remote actions:

- This execution plan and its matching history record.
- Push the current local `main` to `origin/main` after verifying the remote
  has not moved.
- Rename only the `v2.8` and `v2.8.1` Release titles.
- Create and push an annotated `v2.8.3` tag at the verified release commit.
- Create and publish the `v2.8.3` Release through GitHub's API.

Out of scope:

- Creating or publishing a `v2.8.2` Release.
- Changing action source, package metadata, distribution output, or fallback
  credential behavior.
- Altering existing release notes, tags, assets, draft/prerelease status, or
  latest-release selection except where GitHub updates it for `v2.8.3`.

## Baseline

- HEAD: `fc86486959aad3c51b4903044534e6cdc09468a3`
- Branch: `main`, two commits ahead of `origin/main`
- Staged, unstaged, and untracked files: none
- Agent-owned paths: this plan and
  `docs/histories/2026-09/2026-09-04-v2-8-3-release.md`

## Execution

1. Confirm authenticated GitHub access and re-read live release/tag state.
2. Validate the release commit with formatting, tests, build, lint (known
   baseline errors must not increase), package, and whitespace checks.
3. Reconfirm `origin/main` has not moved, then push `main`.
4. Rename the two existing Release titles only.
5. Create and push annotated tag `v2.8.3` at the validated HEAD.
6. Create and publish a `v2.8.3` Release through GitHub's API, then make it
   the latest stable release.
7. Re-read all remote state and record the outcome in the matching history.

## Verification checklist

- [x] Current GitHub credential can perform required release mutations.
- [x] `npm run format-check` passes.
- [x] `npm test -- --runInBand` passes.
- [x] `npm run build` passes.
- [x] `npm run lint` has no new errors beyond the known baseline.
- [x] `npm run package` succeeds without unexpected source or `dist` changes.
- [x] `git diff --check` passes.
- [x] `main`, tag, release target, titles, and latest-release state are
  verified remotely.

## Execution record

- `origin/main` was advanced from `f0170e1` to the validated HEAD `fc86486`.
- Release titles `v2.8` and `v2.8.1` were updated without changing their
  notes, assets, or release state.
- Annotated tag `v2.8.3` was created at `fc86486` and pushed.
- `v2.8.3` was created and published with the requested title and minimal
  release notes, then verified as GitHub's latest stable Release.
- The earlier assumption that each new release required a Marketplace web
  interaction was removed after checking the existing `v2.8.2` path: it is an
  annotated tag without a GitHub Release.
