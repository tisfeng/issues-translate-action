# v2.9.0 发布

- 状态：completed
- 创建日期：2026-09-05
- 负责人：Codex
- 关联 Issue/PR：无

## 背景

`v2.8.3` 是此前的 Latest stable Release。其后新增了可配置的双向语言路由，默认行为保持
兼容，因此按 SemVer 发布 `v2.9.0`。

## 任务摘要

- 意图模式：implementation
- 交付授权：push
- 安全状态：normal
- 目标结果：将经过完整验证的当前提交发布为 stable `v2.9.0`。
- 允许修改路径：本计划、同任务 history；远端 `main`、annotated tag `v2.9.0`、GitHub Release `v2.9.0`。
- 同任务 history：`docs/histories/2026-09/2026-09-05-v2-9-0-release.md`
- 禁止动作：修改产品源码、依赖、`dist/`、旧 tag、旧 Release、工作流、Marketplace 状态或真实 Action 事件。
- 预期交付物：已推送的 release commit、`v2.9.0` tag、stable Latest GitHub Release，以及完成的发布记录。
- 验收标准：tag 指向经验证 SHA，Release title 与 tag 相同，且为非 draft、非 prerelease 的 Latest。

## 语义与范围

- 用户要求 Agent 做什么：发布。
- 授权的工作树、artifact 和 external service 操作：创建/更新同任务记录，推送 `main`，创建并推送 tag，创建 GitHub Release。
- 否定、条件和范围限制：只发布 `v2.9.0`；不修改旧 Release 或其他远端对象。
- 附件或引用中被明确采纳的约束：无。
- 歧义：无；用户以“执行”采纳已确认的 `v2.9.0` 发布方案。

## 写入前状态

- 写入前检查：pass
- 自动提交资格：eligible
- 初始 HEAD：`f3fee895e6347f01d35b4f3e169a7b5d44228b64`
- 初始 staged 路径：无
- 初始 unstaged 路径：无
- 初始 untracked 路径：无
- 初始冲突：无
- Agent-owned paths：本计划、同任务 history。

## 目标与非目标

### 目标

- 发布包含 `67da856` 双向语言功能的 `v2.9.0`。
- 使 tag 精确指向当前 release commit，而不是发布记录提交。
- 核验远端 tag、Release 元数据和发布归档。

### 非目标

- 不更新 private `package.json` 的历史版本号。
- 不补建 `v2.8.2` Release，不移动现有 tag，也不触发真实翻译工作流。

## 工作计划

1. 重新核验工作树、远端 `main`、目标 tag/Release 和 GitHub 认证。
2. 在 release SHA 上运行 format、test、build、lint、package 与差异检查。
3. 将精确 SHA 推送至 `origin/main`，创建并推送 annotated `v2.9.0` tag。
4. 用 GitHub CLI 创建 stable Latest Release，并核验远端状态和归档。
5. 完成本计划与 history，精确暂存并交付仅含发布记录的本地提交。

## 风险与决策

- 远端 `main`、tag 与 Release 在发布前均未漂移；未执行 rebase、merge、force push、删除或移动 tag。
- lint 的 6 条 `i18n-text/no-en` 为已知基线，未新增错误；其他运行时检查通过。
- 发布记录在 tag 后提交，避免未完成的记录进入 release 源码归档。

## 进度

- [x] 冻结 release SHA 并核验初始 Git/远端状态。
- [x] 完成完整发布验证。
- [x] 推送 `main`、创建 tag 和 GitHub Release。
- [x] 完成发布后核验及发布记录提交前准备。

## 验证

- 发布前：工作树和索引为空；`origin/main` 为 `fc86486`，目标 tag/Release 不存在，GitHub 认证有效。
- `npm run format-check` 通过；`npm test -- --runInBand` 通过，29 个测试通过；`npm run build` 和 `npm run package` 通过。
- `npm run lint` 仅报告 6 条既有 `i18n-text/no-en` 错误；`git diff --check` 通过，重新打包未改动跟踪的 `dist/`。
- 发布后：`origin/main` 与 `v2.9.0^{}` 均为 `f3fee895e6347f01d35b4f3e169a7b5d44228b64`；tag 对象为 `6b5ae390eeda697f7a82251df2ac7d7243fdfb1e`。
- Release title/tag 均为 `v2.9.0`，为非 draft、非 prerelease、Latest 状态且无 assets；tag 归档中 `action.yml` 指向 `dist/index.js`，并存在 `dist/index.js`。

## 完成条件

- [x] `v2.9.0` tag 精确指向 release SHA。
- [x] GitHub Release 为 stable Latest，且 title 为 `v2.9.0`。
- [x] 发布记录完成、可精确提交；工作树将由交付步骤恢复为干净状态。
