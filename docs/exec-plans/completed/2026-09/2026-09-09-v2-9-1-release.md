# v2.9.1 发布

- 状态：completed
- 创建日期：2026-09-09
- 完成日期：2026-09-09
- 负责人：Codex

## 任务契约

- 意图模式：implementation
- 交付授权：push
- 目标：将已验证的 `71e9aa61b05d65b9d82faedbe926b5985255f48b` 发布为 stable `v2.9.1`，随后让 Easydict 引用该 tag。
- 允许本仓库写入：本计划、其 completed 归档和 `docs/histories/2026-09/2026-09-09-v2-9-1-release.md`。
- 允许外部操作：快进推送 `origin/main`、创建并推送 annotated tag `v2.9.1`、创建同名 stable GitHub Release。
- 禁止操作：不修改产品源码、依赖、既有 tag/Release、Marketplace 状态，也不触发真实 Action 事件。

## 写入前快照

- `HEAD`：`71e9aa61b05d65b9d82faedbe926b5985255f48b`
- `origin/main`：`799fc37a2a9ee19d3211b99e5d4d5cff1ad37c80`
- staged、unstaged、untracked 和冲突：均为空。
- 目标 tag：`v2.9.1` 不存在；当前 stable Release 为 `v2.9.0`。

## 执行结果

1. `npm run format-check`、42 个 Jest 测试、`npm run build`、`npm run lint`、`npm run package` 和 `git diff --check` 均通过；重新打包未产生跟踪运行时差异。
2. 已将候选 SHA 快进推送到 `origin/main`，并创建、推送 annotated `v2.9.1` tag。
3. 已创建同名公开 stable Release；不含 assets、非 draft、非 prerelease。
4. 发布后确认远端 `main` 与 `v2.9.1^{}` 均为候选 SHA，tag tree 含 `action.yml` 与 `dist/index.js`。

## 完成条件

- [x] `v2.9.1` tag 精确指向已验证的候选 SHA。
- [x] GitHub Release 为 stable，且 title 为 `v2.9.1`。
- [x] 未移动既有 `v2.9.0` tag 或 Release。
