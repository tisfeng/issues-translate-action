# v2.8.3 Release History

- Date: 2026-09-04
- Status: completed
- Related plan: `docs/exec-plans/completed/2026-09-04-v2-8-3-release.md`

## Completed

- Validated release commit `fc86486` with format, test, build, package, and
  whitespace checks. The repository's known lint baseline remains 11 errors;
  no new lint errors were introduced.
- Pushed the validated `main` to `origin/main`.
- Renamed only the existing `v2.8` and `v2.8.1` GitHub Release titles.
- Created and pushed annotated tag `v2.8.3` at `fc86486`.
- Created and published `v2.8.3` with no assets, then marked it as the latest
  stable GitHub Release.
- Verified the public Release title, tag, target, stable status, latest status,
  and the unchanged `v2.8` and `v2.8.1` Release titles.

## Correction

The initial assumption that Marketplace publication required a manual web
interaction was incorrect for this task. The prior `v2.8.2` publication is an
annotated tag without a GitHub Release, so the final `v2.8.3` Release was
published through GitHub's API without user intervention.
