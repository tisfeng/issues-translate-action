# v2.9.0 发布记录

- 日期：2026-09-05
- 状态：completed
- 关联 Issue/PR：无
- 执行计划：`docs/exec-plans/completed/2026-09-05-v2-9-0-release.md`

## 用户目标

发布 `v2.9.0`，将当前经过验证的双向语言翻译功能作为新的稳定 GitHub Action 版本。

## 设计意图

- 使用与 tag 相同的 Release 标题，并让 tag 固定指向已验证的 release SHA。
- 将发布记录作为 tag 之后的独立文档提交，避免把进行中的记录纳入 release 源码归档。

## 主要变更

- 将 `f3fee895e6347f01d35b4f3e169a7b5d44228b64` 推送到 `origin/main`。
- 创建并推送 annotated tag `v2.9.0`；其 tag 对象为
  `6b5ae390eeda697f7a82251df2ac7d7243fdfb1e`，peeled SHA 为 release SHA。
- 创建 title 为 `v2.9.0` 的公开 stable GitHub Release，设为 Latest 且不上传 assets。
- 归档发布计划，并用本记录保存发布事实；该文档提交不包含在 `v2.9.0` tag 中。

## 验证

- 初始工作树、索引和冲突均为空，目标 tag/Release 不存在，GitHub 认证有效。
- `npm run format-check`、29 个 Jest 测试、`npm run build`、`npm run package` 和
  `git diff --check` 通过；重新打包未改动跟踪的 `dist/`。
- `npm run lint` 仍只报告 6 条既有 `i18n-text/no-en` 错误，没有新增 lint 类型。
- 远端 `main`、`v2.9.0^{}` 均为 release SHA；Release 为非 draft、非 prerelease、Latest 且
  无 assets；tag 归档内已确认 `action.yml` 和 `dist/index.js`。

## 后续事项

- 无。
